GEMINI_API_KEY=your_gemini_api_key_here

# Optional: max number of videos generated concurrently (Veo 3.1 is slow/metered)
MAX_CONCURRENT_JOBS=2

# Optional: max /generate requests a single client IP can submit (Flask-Limiter syntax, e.g. "5 per hour")
GENERATE_RATE_LIMIT=5 per hour
